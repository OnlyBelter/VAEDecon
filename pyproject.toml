# pyproject.toml

[build-system]
requires = ["setuptools>=61.0.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "VAEDecon"
version = "0.4.0"
description = "Gene expression deconvolution by single cell generative model"
readme = "README.md"
license = "MIT"
requires-python = ">=3.11"
authors = [
    { name = "OnlyBelter", email = "onlybelter@outlook.com" },
]
maintainers = [
    { name = "OnlyBelter", email = "onlybelter@outlook.com" },
]
keywords = [
    "VAE",
    "scRNA-seq",
    "RNA-seq",
    "deconvolution",
    "tumor microenvironment",
    "bioinformatics",
    "computational biology",
]
classifiers = [
    "Intended Audience :: Science/Research",
#    "License :: OSI Approved :: MIT License",
    "Programming Language :: Python",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Typing :: Typed",
]
dependencies = [
#    "tensorflow>=2.8.0",
#    "torch==2.6.0",
    "torch==2.11.0",
    "torchvision==0.26.0",
    "scikit-learn==1.7.2",
    "anndata==0.8.0",
    "scanpy==1.11.5",
    "pandas==3.0.2",
    "numpy==2.4.3",
    "numexpr==2.14.1",
    "scipy==1.16.3",
    "umap-learn==0.5.6",
    "matplotlib==3.10.7",
    "seaborn==0.13.2",
    "bbknn==1.5.1",
    "SciencePlots",
    "tables==3.10.2",
    "build==1.3.0",
    "sphinx==7.3.7",
    "myst-parser==4.0.1",
    "fsspec",
    "h5py==3.16.0",
    "huggingface_hub",
#    "sphinx_book_theme",
    "jinja2==3.1.6",
    "pydantic",
    "pyyaml",
    "cloudpickle",
#    "pythae",
    "torchmetrics",
    "lightning==2.5.1",
    "torch-geometric==2.6.1",
    "networkx==3.6.1",
    "igraph==0.11.8",
    "statsmodels==0.14.6",

#    'tensorrt',
#    "sphinx_rtd_theme"
]

[project.optional-dependencies]
test = [
    "pytest",
]
#dynamic = ["version"]

[project.urls]
#Documentation = "https://deside.readthedocs.io/"
Homepage = "https://github.com/OnlyBelter/VAEDecon"
Source = "https://github.com/OnlyBelter/VAEDecon"
Tracker = "https://github.com/OnlyBelter/VAEDecon/issues"

[project.scripts]
vaedecon = "vaedecon.cli.main:main"

# Setuptools configuration
[tool.setuptools.packages.find]
where = ["."]
include = ["vaedecon*"]
exclude = ["tests*", "docs*", "examples*", "configs*"]  # Exclude configs and examples from the package

[tool.setuptools.package-data]
"vaedecon.configs" = ["*.yaml"]

# https://realpython.com/pypi-publish-python-package/#version-your-package
[tool.bumpver]
current_version = "0.1.0"
version_pattern = "MAJOR.MINOR.PATCH"
commit_message = "bump version {old_version} -> {new_version}"
commit = true
tag = true
push = false

[tool.bumpver.file_patterns]
"pyproject.toml" = [
    'current_version = "{version}"',
]
"setup.py" = [
    "{version}",
    "{pep440_version}",
]
"README.rst" = [
    "{version}",
    "{pep440_version}",
]
"README.md" = [
    "{version}",
    "{pep440_version}",
]
